# ==============================================================================
# Environment Configuration
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# .env is gitignored and should NEVER be committed.
# This file (.env.example) IS committed -- it documents every variable.
# ==============================================================================

# ------------------------------------------------------------------------------
# Application
# ------------------------------------------------------------------------------

# The environment this instance is running in.
# Values: development | test | production
APP_ENV=development

# Log verbosity.
# Values: debug | info | warn | error
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# Add your project-specific variables below.
# For each variable, document:
#   - What it does
#   - Whether it is required or optional
#   - The expected format or allowed values
#   - A safe example value (never a real secret)
# ------------------------------------------------------------------------------

# Example: database connection string (required in production)
# DATABASE_URL=postgres://user:password@localhost:5432/mydb

# Example: secret key for signing tokens (required, min 32 chars)
# SECRET_KEY=change-me-to-a-long-random-string

# Example: external API base URL (optional, has a default)
# API_BASE_URL=https://api.example.com
